# ─── Banco de dados (PostgreSQL) ───
# Local (docker-compose ou Postgres instalado):
DATABASE_URL="postgresql://bakaninha:bakaninha@localhost:5432/bakaninha?schema=public"
# Produção (VPS): troque pelos dados reais do servidor
# DATABASE_URL="postgresql://USER:PASS@HOST:5432/bakaninha?schema=public"

# ─── Credenciais admin ───
ADMIN_USERNAME=bakaninha@admin.com
ADMIN_PASSWORD=bakana2026!

# ─── JWT ───
# Em produção, gere uma chave forte com: openssl rand -base64 48
JWT_SECRET=troque_por_uma_chave_segura_aqui

# ─── Servidor ───
PORT=3001
# Lista de origens permitidas (separadas por vírgula). Use '*' apenas em dev.
# Em produção (VPS): CORS_ORIGIN=https://bakaninha.netlify.app
CORS_ORIGIN=*

# ─── WhatsApp (somente números, com DDI) ───
WHATSAPP_NUMBER=553838411604
